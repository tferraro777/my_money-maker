// Prisma intentionally not used for schema migrations.
// Database schema is managed via db/schema.sql.
// Do not add models here unless architecture decision changes.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
